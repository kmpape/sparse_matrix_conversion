# sparse_matrix_conversion

Utilities to convert dense matrix formats to sparse matrix formats and the reverse.
Code is not optimized for performance nor for memory usage.

Define UNIT_TESTS to run unit tests.
Undefine UNIT_TESTS to run example.
